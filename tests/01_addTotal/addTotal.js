// make sure to check readme for information about taxes
function addTotal(arr) {
  return filtered = arr.filter(addTotal => {
   return cart.price.addTotal;
  });
  console.log(filtered);
  return filtered.reduce(accum, iter) => {
    return accum + iter.addTotal
  }
}

module.exports = { addTotal };
